language: go

go:
  - tip
  - master

before_script:
  - go get -t -d -v ./... 

script:
  - go build -v ./...
  - go test -cpu=1,2 -v -tags unit ./...
  - go test -race -coverprofile=coverage.txt -covermode=atomic

after_success:
  - bash <(curl -s https://codecov.io/bash)